def shorten(string):
    lista = []
    for char in string:
        if char.isupper():
            lista.append(char)

    return "".join(lista)

def main():
    string = input("Escree algo que só vão fiar as letras maiúsculas: ")
    
    print(shorten(string))

main()